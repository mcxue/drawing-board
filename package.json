{
  "scripts": {
    "serve": "parcel serve src/index.html",
    "build": "rm -rf dist && parcel build src/index.html --public-url ./"
  },
  "dependencies": {
    "reset.css": "^2.0.2",
    "src": "^1.1.2"
  },
  "devDependencies": {
    "cssnano": "^4.1.10"
  }
}